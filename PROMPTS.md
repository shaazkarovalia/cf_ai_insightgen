# AI Prompts Used

I used AI to assist in generating the SQL schema and the CSS for the frontend.

## Prompt 1: SQL Schema
> "Create a SQLite schema for a simple table that stores a topic, a summary, and a timestamp."

## Prompt 2: HTML/CSS
> "Write a simple HTML page with vanilla JavaScript that fetches data from an API endpoint and displays it in a list. Style it cleanly using system fonts."